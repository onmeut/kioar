// Stubs for Node test runs. `server-only` is a Next.js bundler hook; in
// tests we simply make it a no-op so modules that `import "server-only"`
// can be imported without bundler magic.
export {};
